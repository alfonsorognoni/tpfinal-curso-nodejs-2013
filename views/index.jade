extends layout

block content
  h1= title
  p Welcome to #{title}
  label(for='search') Search
  input#email(name='search', placeholder='Employee', value='')
  input#send(type='submit', value='Search')